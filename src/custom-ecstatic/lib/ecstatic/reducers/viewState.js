import { HIDE_VIEW, SHOW_VIEW, FADE_IN_VIEW, FADE_OUT_VIEW } from '../actions/viewState'

export default function viewState(state = {}, action) {
    switch(action.type) {
        case HIDE_VIEW :

    }
}

// TODO: need to define interchangebable views
let defaultState = {

}
