import styled from 'styled-components'

export const FilesContainer = styled.div`
    width: ${props => props.width};
    margin-left: auto;
    margin-right: auto;
    padding-top: 130px;
    position: relative;
`